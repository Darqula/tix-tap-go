import { Box } from "@mui/material";
import { styles } from "./Logo.styles";

const Logo = () => {
  return (
    <Box sx={styles}>
      Tix
      <em>Tap</em>
      Go Admin
    </Box>
  );
};

export { Logo };
